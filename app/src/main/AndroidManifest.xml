<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.mentoring_app">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mentoring_App">
        <activity android:name=".activties.LoginActivity" />
        <activity android:name=".activties.WelcomeCoordinatorActivity" />
        <activity android:name=".activties.WelcomeMentTrialActivty" />
        <activity android:name=".activties.WelcomeStudentActivity" />
        <activity android:name=".activties.ForgotPasswordActivity" />
        <activity android:name=".activties.CreateAccountActivity" />
        <activity android:name=".activties.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>